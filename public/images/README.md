# Images

All wired up via `lib/data.ts`:

- `hero.jpg` — tour bus at dusk, shown beside the hero copy
- `logo.png` / `logo-mark.png` — brand lockup and cropped emblem (emblem also
  serves as the favicon via `app/icon.png`)
- `team/*.jpg` — leadership headshots
